fastapi==0.115.0
uvicorn[standard]==0.32.0
aiosqlite==0.20.0
jinja2==3.1.4
python-multipart==0.0.12
httpx==0.27.2
itsdangerous==2.1.2
bcrypt==5.0.0

# Nightly workout enrichment (scripts/enrich_workouts.py)
anthropic==0.103.1

# SQLCipher: whole-database encryption at rest
# Requires: sudo apt install libsqlcipher-dev  (then pip install pysqlcipher3)
# Only active when DB_ENCRYPTION_KEY is set in .env; tests run unencrypted if unset.
pysqlcipher3

# Test dependencies
pytest==8.3.3
pytest-asyncio==0.24.0
